apiVersion: apps/v1
kind: Deployment
metadata:
  name: fill-eight-gpus
  namespace: kai-lab12
  labels:
    kai-scheduler-101/chapter: "12"
spec:
  replicas: 8
  selector:
    matchLabels:
      app: fill-eight-gpus
  template:
    metadata:
      labels:
        app: fill-eight-gpus
        kai.scheduler/queue: gang-demo
        kai-scheduler-101/chapter: "12"
    spec:
      schedulerName: kai-scheduler
      priorityClassName: train
      tolerations: [ { key: kwok.x-k8s.io/node, operator: Exists, effect: NoSchedule } ]
      resourceClaims: [ { name: gpu, resourceClaimTemplateName: gpu-one } ]
      containers:
        - name: worker
          image: ubuntu:22.04
          command: ["bash", "-lc"]
          args: ["echo occupying one simulated GPU; sleep 3600"]
          resources: { claims: [ { name: gpu } ] }

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: fill-four-gpus
  namespace: kai-lab12
  labels:
    kai-scheduler-101/chapter: "12"
spec:
  replicas: 4
  selector:
    matchLabels:
      app: fill-four-gpus
  template:
    metadata:
      labels:
        app: fill-four-gpus
        kai.scheduler/queue: gang-demo
        kai-scheduler-101/chapter: "12"
    spec:
      schedulerName: kai-scheduler
      priorityClassName: train
      tolerations: [ { key: kwok.x-k8s.io/node, operator: Exists, effect: NoSchedule } ]
      resourceClaims: [ { name: gpu, resourceClaimTemplateName: gpu-one } ]
      containers:
        - name: worker
          image: ubuntu:22.04
          command: ["bash", "-lc"]
          args: ["echo occupying one simulated GPU; sleep 3600"]
          resources: { claims: [ { name: gpu } ] }
