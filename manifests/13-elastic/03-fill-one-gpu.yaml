apiVersion: v1
kind: Pod
metadata:
  name: elastic-fill-one
  namespace: kai-lab13
  labels:
    app: elastic-fill-one
    kai.scheduler/queue: elastic-demo
    kai-scheduler-101/chapter: "13"
spec:
  schedulerName: kai-scheduler
  restartPolicy: Never
  tolerations: [ { key: kwok.x-k8s.io/node, operator: Exists, effect: NoSchedule } ]
  resourceClaims: [ { name: gpu, resourceClaimTemplateName: gpu-one } ]
  containers:
    - name: worker
      image: ubuntu:22.04
      command: ["bash", "-lc"]
      args: ["echo occupying one simulated GPU; sleep 3600"]
      resources: { claims: [ { name: gpu } ] }
